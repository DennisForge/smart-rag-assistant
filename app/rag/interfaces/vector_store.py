# TODO (F3): Define VectorStoreInterface with abstract methods:
# - add(chunk_id, document_id, embedding, text, metadata)
# - add_batch(...)
# - query(query_embedding, top_k, filters)
# - delete_by_document_id(document_id)
# - delete_by_chunk_id(chunk_id)
# - count()
# - clear()
